# Cinema TMS Admin 1.3.0 Beta 2

- VBS 실행기를 콘솔용 `python.exe` 숨김 실행에서 GUI용 `pythonw.exe` 정상 창 실행으로 변경했습니다.
- Windows Tk 기본 글꼴을 `맑은 고딕`으로 지정해 한글 IME 조합 문자와 커서 표시를 안정화했습니다.
- 관리자 DB, 로그인 계정, 암호화된 발급키와 발급·감사 이력은 변경하지 않습니다.

기존 기능의 Windows 한글 입력 호환성 수정이므로 `1.3.0b1`에서 `1.3.0b2`로 패치 버전을 올렸습니다.
